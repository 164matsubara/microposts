    <% if @users.any? %>
    
        <% @users.each do |user| %>
        <li><%= link_to gravatar_for(user, size: 50), user %></li>
        <li><%= link_to "#{user.name}", user %></li>
        <li><% user.profile %></li>
        <% end %>
    <%= will_paginate%>
<% end %>